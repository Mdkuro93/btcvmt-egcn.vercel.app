src/components/modals/BulkIssueModal.tsx:119:              ] as const).map(({ value, label }) => (
src/components/modals/CreateApplicationModal.tsx:103:                          {visibleProjects.map((p, pIdx) => (
src/components/modals/ProjectModal.tsx:149:                  {(formData.originalDocumentChecklist || []).map((item, idx) => (
src/components/modals/ImportPreviewModal.tsx:98:                {errors.map((e, i) => (
src/components/modals/ImportPreviewModal.tsx:111:                {duplicateFileWarnings.map((w, i) => (
src/components/modals/ImportPreviewModal.tsx:124:                {crossProjectWarnings.map((w, i) => (
src/components/modals/ImportPreviewModal.tsx:137:                {otherWarnings.map((w, i) => (
src/components/modals/ApplicationDetailModal.tsx:685:                      {["01", "02", "03", "04", "05", "06", "07"].map(
src/components/modals/ApplicationDetailModal.tsx:1113:                                  ].map((item, idx) => {
src/components/modals/ApplicationDetailModal.tsx:1740:                                  const h = rawHistory.map((entry) => ({
src/components/modals/ApplicationDetailModal.tsx:1753:                                  const a = rawAudit.map((entry) => ({
src/components/modals/ApplicationDetailModal.tsx:1784:                                  return merged.map((log, index) => (
src/components/modals/ApplicationDetailModal.tsx:1870:                              {(editApp || selectedApp).scannedFiles?.map(
src/components/modals/UserManagementModal.tsx:63:                      ].map(p => (
src/components/modals/UserManagementModal.tsx:160:                    {projects.map((project, index) => {
src/components/HandoverRecord.tsx:52:            {apps.map((app, idx) => (
src/components/HandoverRecord.tsx:64:                        {app.scannedFiles.map((f, fIdx) => (
src/components/HandoverRecord.tsx:75:                      {app.scannedFiles.map((f, fIdx) => (
src/components/HandoverRecord.tsx:91:            {apps.length === 0 && Array.from({length: 5}).map((_, i) => (
src/components/ProjectManagementView.tsx:100:          .map(([region, regionProjects], idx) => (
src/components/ProjectManagementView.tsx:145:                    {regionProjects.map((project, pIdx) => (
src/components/ReportsView.tsx:135:  const [selectedLoanProjectIds, setSelectedLoanProjectIds] = useState<string[]>(projects.map(p => p.id));
src/components/ReportsView.tsx:191:        ).map(p => {
src/components/ReportsView.tsx:239:        ...filteredApps.map(app => {
src/components/ReportsView.tsx:279:        ...filteredApps.map((app, i) => [
src/components/ReportsView.tsx:302:    const colWidths = (rows[3] || []).map((_: any, i: number) => ({
src/components/ReportsView.tsx:305:        ...rows.slice(3).map((r: any) => 
src/components/ReportsView.tsx:368:      return projects.map(p => {
src/components/ReportsView.tsx:381:      return REGION_ORDER.map(reg => {
src/components/ReportsView.tsx:396:      return projects.filter(p => selectedLoanProjectIds.includes(p.id)).map(p => {
src/components/ReportsView.tsx:411:      return depts.map(dept => {
src/components/ReportsView.tsx:418:           const overdue = applications.filter(app => app.id === a.id).map(app => {
src/components/ReportsView.tsx:458:       return Object.entries(userStats).map(([id, s]) => ({
src/components/ReportsView.tsx:473:    return steps.map(step => {
src/components/ReportsView.tsx:569:          {(Object.keys(reportConfig) as Array<keyof typeof reportConfig>).map((type, idx) => (
src/components/ReportsView.tsx:633:          {(Object.keys(reportConfig) as Array<keyof typeof reportConfig>).map((type, idx) => (
src/components/ReportsView.tsx:690:          {projects.map((p, pIdx) => (
src/components/ReportsView.tsx:753:                  {reportConfig[reportType].roles.map((roleText, idx) => (
src/components/ReportsView.tsx:760:               {reportConfig[reportType].kpis.map((kpiText, i) => (
src/components/ReportsView.tsx:809:                         onClick={() => setSelectedLoanProjectIds(projects.map(p => p.id))}
src/components/ReportsView.tsx:820:                    {projects.map((p, pIdx) => {
src/components/ReportsView.tsx:900:                                 {loanPieData.map((entry: any, index: number) => (
src/components/ReportsView.tsx:956:                                 {loanPieData.map((entry: any, index: number) => (
src/components/ReportsView.tsx:998:                      {loanApps.map((app, index) => {
src/components/ReportsView.tsx:1123:                         {stats.length > 0 ? stats.slice().sort((a:any, b:any) => b.total - a.total).map((user: any, i: number) => (
src/components/ReportsView.tsx:1154:                    {stats.slice(0, 4).map((user: any, index: number) => (
src/components/ReportsView.tsx:1261:                {stats.slice(0, 4).sort((a,b) => b.completed - a.completed).map((p, i) => (
src/components/tabs/ResourcesTab.tsx:54:                      {DOC_CHECKLIST_ITEMS.map((item, idx) => (
src/components/tabs/ResourcesTab.tsx:121:                        ].map((doc, idx) => (
src/components/tabs/DashboardTab.tsx:351:                              {progressChartData.map((entry: any, index: number) => (
src/components/tabs/DashboardTab.tsx:463:                                {overallPieData.map((entry: any, index: number) => (
src/components/tabs/DashboardTab.tsx:520:                                             {roleKpis.loanRatioStats.map((entry: any, index: number) => (
src/components/tabs/DashboardTab.tsx:559:                                             {loanPieData.map((entry: any, index: number) => (
src/components/tabs/DashboardTab.tsx:628:                        {roleKpis.admin.deptStats.map((dept: any, idx: number) => (
src/components/tabs/DashboardTab.tsx:683:                          {REGION_ORDER.map((r: string, index: number) => (
src/components/tabs/DashboardTab.tsx:704:                          .map((p: any, index: number) => {
src/components/tabs/ApplicationsTab.tsx:231:                          ] as const).map(({ field, label }, idx) => (
src/components/tabs/ApplicationsTab.tsx:320:                                  ].map((item, idx) => {
src/components/tabs/ApplicationsTab.tsx:476:                              {activeFilters.map((act, idx) => (
src/components/tabs/ApplicationsTab.tsx:777:                            EDITABLE_DATE_FIELDS.map((f, index) => (
src/components/tabs/ApplicationsTab.tsx:823:                        ) : currentVisibleApps.map((app, index) => {
src/components/tabs/ApplicationsTab.tsx:1019:                                EDITABLE_DATE_FIELDS.map((f, fIdx) => {
src/components/tabs/ApplicationsTab.tsx:1431:                    assignableUsers.map((u: any, index: number) => (
src/components/Sidebar.tsx:370:            .map(([region, regionProjects], regIndex) => (
src/components/Sidebar.tsx:416:                      {regionProjects.map((p, pIndex) => (
src/components/FieldModeView.tsx:142:    return allowedProjects.map(p => p.name);
src/components/FieldModeView.tsx:463:          {allowedProjects.map((p, index) => (
src/components/FieldModeView.tsx:543:            filteredApps.map((app, index) => {
src/components/FieldModeView.tsx:845:                               ].map((st) => {
src/components/FieldModeView.tsx:1043:                                               {backwardSteps.map((st, idx) => (
src/components/FieldModeView.tsx:1099:                                  {appWorkflowSteps.map((stepKey, idx) => {
src/components/FieldModeView.tsx:1190:                                {editAppInstance.scannedFiles.map((file, i) => (
src/components/NotificationPanel.tsx:108:              {taskReminders.map((rem, index) => (
src/components/NotificationPanel.tsx:147:            displayedNotifications.map((n, idx) => (
src/components/UserManagementView.tsx:147:                {filteredUsers.map((user, idx) => (
src/components/DashboardCharts.tsx:127:                  {chartData.map((entry, index) => (
src/components/DashboardCharts.tsx:137:                  {chartData.map((entry, index) => (
src/components/DashboardCharts.tsx:195:                  {pieData.map((entry, index) => (
src/components/DashboardCharts.tsx:229:            {pieData.map((entry, index) => (
src/components/ResourcesCenter.tsx:29:         {categories.map(cat => (
src/components/ResourcesCenter.tsx:66:           {[1, 2, 3, 4, 5].map(i => (
src/components/ErrorReportView.tsx:34:    return applications.map(app => {
src/components/ErrorReportView.tsx:60:      .map(app => {
src/components/ErrorReportView.tsx:101:    return Object.entries(errorSummary).map(([key, value]) => ({
src/components/ErrorReportView.tsx:115:    return Object.entries(stepStats).map(([key, value]) => ({
src/components/ErrorReportView.tsx:263:                    {severityChartData.map((entry, index) => (
src/components/ErrorReportView.tsx:325:                    {typeChartData.map((entry, index) => (
src/components/ErrorReportView.tsx:363:                Object.keys(errorSummary).map((type, idx) => {
src/components/ErrorReportView.tsx:431:                  {stepChartData.map((entry, index) => (
src/components/ErrorReportView.tsx:482:                mappedAppsForIssues.map((app, index) => (
src/components/LoginScreen.tsx:67:              .map((r: any) => r.project_name)
src/components/LoginScreen.tsx:125:          assignedProjectIds: PROJECTS.map(p => p.id),
src/components/LoginScreen.tsx:423:                      {stepStats.slice(0, 3).map((step, idx) => (
src/components/AppSubComponents.tsx:150:                  options.map((opt, idx) => <option key={`${opt}-${idx}`} value={opt}>{opt}</option>)
src/components/AppSubComponents.tsx:190:    {[1, 2, 3, 4, 5, 6].map((i) => (
src/components/SettingsView.tsx:168:                  .map(([step, days], idx) => (
src/components/SettingsView.tsx:212:                  .map(([step, days], idx) => (
src/components/SettingsView.tsx:284:              {checklistTemplates.map((item, idx) => (
src/components/SettingsView.tsx:648:                {workflowSequences[workflowTab].map((key, index) => {
src/components/ThemeToggle.tsx:70:           {[...Array(6)].map((_, i) => (
src/components/SLAReportView.tsx:128:      scopedApps.map(a => ({
src/components/SLAReportView.tsx:156:    return PROGRESS_STAGES.map(stage => {
src/components/SLAReportView.tsx:163:      const maxCount = Math.max(...PROGRESS_STAGES.map(s =>
src/components/SLAReportView.tsx:179:    return projects.map(proj => {
src/components/SLAReportView.tsx:181:      const withSLA = projApps.map(a => ({
src/components/SLAReportView.tsx:265:            {['Mã hồ sơ', 'Dự án', 'Vào bước', 'Trễ', 'Trạng thái'].map(h => (
src/components/SLAReportView.tsx:272:          {pagedApps.map((app, index) => {
src/components/SLAReportView.tsx:327:          {projects.filter(p => deptApps.some(a => a.projectName === p.name)).map((proj, index) => (
src/components/SLAReportView.tsx:350:          ].map((k, index) => (
src/components/SLAReportView.tsx:361:            {projectStats.map((ps, index) => (
src/components/SLAReportView.tsx:417:              {['Giai đoạn / Bộ phận', 'Số hồ sơ đang kẹt', 'Trễ SLA', 'Avg trễ'].map(h => (
src/components/SLAReportView.tsx:423:          {stageStats.map((stage, idx) => (
src/hooks/useBulkActions.ts:46:      const updatedApps = applications.map(app => {
src/hooks/useBulkActions.ts:77:      const updatedApps = appsToUpdate.map(app => 
src/hooks/useBulkActions.ts:104:      const updatedApps = applications.map(app => {
src/hooks/useExcelImport.ts:134:      data = sourceApps.map((app: Application) => [
src/hooks/useExcelImport.ts:164:      data = sourceApps.map((app: Application) => [
src/hooks/useExcelImport.ts:185:    worksheet['!cols'] = headers.map(() => ({ wch: 20 }));
src/hooks/useExcelImport.ts:831:        ...importPreviewData.toUpdate.map(t => t.app),
src/hooks/useExcelImport.ts:832:        ...importPreviewData.toCreate.map(t => t.app)
src/utils/appUtils.ts:218:    .map(d => ({ ...d, value: app[d.key as keyof Application] }))
src/App.tsx:307:    const historyPromises = app.history.map(h => {
src/App.tsx:326:    const auditPromises = app.auditTrail.map(a => {
src/App.tsx:384:      result.history = historyData.map((h: any) => ({
src/App.tsx:402:        result.history = hData.map((h: any) => ({
src/App.tsx:427:      result.auditTrail = auditData.map((a: any) => ({
src/App.tsx:442:        result.auditTrail = aData.map((a: any) => ({
src/App.tsx:487:      .map(app => {
src/App.tsx:495:      .map(app => mapToSnakeCase(app));
src/App.tsx:657:      .map(fa => fa.id);
src/App.tsx:1029:    return (dashboardApps || []).map(a => ({
src/App.tsx:1299:        if (usersData) setUsers(usersData.map(mapUserFromSnakeCase));
src/App.tsx:1326:      .map(p => p.name);
src/App.tsx:1440:              handleSetApplications(prev => prev.map(a => 
src/App.tsx:1443:              handleSetDashboardApps(prev => prev.map(a => 
src/App.tsx:1611:        const assignedNames = projects.filter(p => currentUser.assignedProjectIds?.includes(p.id)).map(p => p.name);
src/App.tsx:1898:      const fetchedApps = (data || []).map(mapFromSnakeCase);
src/App.tsx:1976:          const assignedNames = projects.filter(p => currentUser.assignedProjectIds.includes(p.id)).map(p => p.name);
src/App.tsx:1985:      const fetched = (data || []).map(mapFromSnakeCase);
src/App.tsx:2091:        .map(file => file.name);
src/App.tsx:2125:        setNotifications(data.map(mapNotificationFromSnakeCase));
src/App.tsx:2167:        targetUsers.map(u => createNotification({
src/App.tsx:2227:      setNotifications(prev => prev.map(n => n.id === id ? { ...n, isRead: true } : n));
src/App.tsx:2244:      setNotifications(prev => prev.map(n => ({ ...n, isRead: true })));
src/App.tsx:2621:          const allIds = displayedApps.map(a => a.id);
src/App.tsx:2634:            const dataRows = rowsToCopy.map(r => [
src/App.tsx:2719:      handleSetApplications(prev => prev.map(a => a.id === id ? finalApp : a));
src/App.tsx:2720:      handleSetDashboardApps(prev => prev.map(a => a.id === id ? finalApp : a));
src/App.tsx:2749:        const updatedApps = apps.map(app => {
src/App.tsx:2778:        handleSetApplications(prev => prev.map(a => {
src/App.tsx:2783:        handleSetDashboardApps(prev => prev.map(a => {
src/App.tsx:2919:          .map(d => ({ ...d, value: app[d.key as keyof Application] }))
src/App.tsx:3202:        const finalUpdatedApps = await bulkSyncRecordsToSupabase(updatePayloads.map(p => mapFromSnakeCase(p)), updatedAppsLocal);
src/App.tsx:3409:      data = sourceApps.map(app => [
src/App.tsx:3440:      data = sourceApps.map(app => {
src/App.tsx:3468:      data = sourceApps.map(app => [
src/App.tsx:3496:      data = sourceApps.map(app => [
src/App.tsx:3516:      data = sourceApps.map(app => [
src/App.tsx:3607:      handleSetApplications(prev => prev.map(app => app.id === oldId ? finalApp : app));
src/App.tsx:3608:      handleSetDashboardApps(prev => prev.map(app => app.id === oldId ? finalApp : app));
src/App.tsx:3815:      handleSetApplications(prev => prev.map(a => a.id === app.id ? finalApp : a));
src/App.tsx:3816:      handleSetDashboardApps(prev => prev.map(a => a.id === app.id ? finalApp : a));
src/App.tsx:3982:      const updatedApps = applications.map(app => {
src/App.tsx:4220:      const updatedApps = appsToResolve.map(app => {
src/App.tsx:4254:        const validIds = new Set(finalApps.map(a => a.id));
src/App.tsx:4255:        return prev.map(a => validIds.has(a.id) ? finalApps.find(f => f.id === a.id) || a : a);
src/App.tsx:4318:      handleSetApplications(prev => prev.map(a => a.id === app.id ? finalWithAudit : a));
src/App.tsx:4319:      handleSetDashboardApps(prev => prev.map(a => a.id === app.id ? finalWithAudit : a));
src/App.tsx:4368:      handleSetApplications(prev => prev.map(a => a.id === app.id ? finalWithAudit : a));
src/App.tsx:4369:      handleSetDashboardApps(prev => prev.map(a => a.id === app.id ? finalWithAudit : a));
src/App.tsx:4419:        .map(app => ({
src/App.tsx:4435:      handleSetDashboardApps(prev => prev.map(a => {
src/App.tsx:4514:      handleSetApplications(prev => prev.map(a => a.id === app.id ? finalApp : a));
src/App.tsx:4560:      handleSetApplications(prev => prev.map(a => a.id === app.id ? finalApp : a));
src/App.tsx:4610:      handleSetApplications(prev => prev.map(a => a.id === appId ? finalApp : a));
src/App.tsx:4611:      handleSetDashboardApps(prev => prev.map(a => a.id === appId ? finalApp : a));
src/App.tsx:4664:      const promises = targetUsers.map(u => 
src/App.tsx:4678:      handleSetApplications(prev => prev.map(a => a.id === oldId ? finalApp : a));
src/App.tsx:4679:      handleSetDashboardApps(prev => prev.map(a => a.id === oldId ? finalApp : a));
src/App.tsx:4708:      const updatedApps = appsToReject.map(app => {
src/App.tsx:4750:        return targetUsers.map(u => ({
src/App.tsx:4761:          const snakeNotis = notificationsToInsert.map(n => {
src/App.tsx:4775:        const validIds = new Set(finalApps.map(a => a.id));
src/App.tsx:4776:        return prev.map(a => validIds.has(a.id) ? finalApps.find(f => f.id === a.id) || a : a);
src/App.tsx:4956:      handleSetApplications(prev => prev.map(app => {
src/App.tsx:5191:      setUsers(prev => prev.map(u => u.id === editUser.id ? editUser : u));
src/App.tsx:5226:      setUsers(prev => prev.map(usr => usr.id === u.id ? updatedUser : usr));
src/App.tsx:5291:        .map(a => ({ ...a, overdue: getOverdueInfo(a, stepConfig, slaConfig) }))
src/App.tsx:5359:    const deptStats = depts.map(dept => {
src/App.tsx:5390:    const adminSlaStats = adminSlaStages.map(stage => {
src/App.tsx:5577:            setSelectedAppIds(displayedApps.map(a => a.id));
src/App.tsx:5761:    return chartData.map(d => {
src/App.tsx:5776:    return chartData.map(d => ({
src/App.tsx:5857:          handleSetApplications(prev => prev.map(a => a.id === updated.id ? updated : a));
src/App.tsx:6188:                {(currentUser?.name || 'User').split(' ').map(n => n[0]).join('').slice(0, 2)}
src/App.tsx:6568:            {printHandoverApps.map((app, index) => (
src/App.tsx:6627:                updatedProjects = projects.map(proj => proj.id === editingProject.id ? updated as Project : proj);
src/App.tsx:6694:        unitCodes={applications.filter(a => selectedAppIds.includes(a.id)).map(a => a.unitCode)}
src/App.tsx:6717:        unitCodes={applications.filter(a => selectedAppIds.includes(a.id)).map(a => a.unitCode)}
src/App.tsx:6830:                        {visibleProjects.map((p, index) => (
