src/components/tabs/ApplicationsTab.tsx:204:                          ] as const).map(({ field, label }, idx) => (
src/components/tabs/ApplicationsTab.tsx:293:                                  ].map((item, idx) => {
src/components/tabs/ApplicationsTab.tsx:449:                              {activeFilters.map((act, idx) => (
src/components/tabs/ApplicationsTab.tsx:695:                                  const allIds = filteredApps.map(a => a.id);
src/components/tabs/ApplicationsTab.tsx:709:                            EDITABLE_DATE_FIELDS.map((f, index) => (
src/components/tabs/ApplicationsTab.tsx:755:                        ) : displayedApps.slice(currentPage * pageSize, (currentPage + 1) * pageSize).map((app, index) => {
src/components/tabs/ApplicationsTab.tsx:927:                                EDITABLE_DATE_FIELDS.map((f, fIdx) => {
src/components/tabs/DashboardTab.tsx:351:                              {progressChartData.map((entry, index) => (
src/components/tabs/DashboardTab.tsx:463:                                {overallPieData.map((entry, index) => (
src/components/tabs/DashboardTab.tsx:520:                                             {roleKpis.loanRatioStats.map((entry: any, index: number) => (
src/components/tabs/DashboardTab.tsx:559:                                             {loanPieData.map((entry, index) => (
src/components/tabs/DashboardTab.tsx:628:                        {roleKpis.admin.deptStats.map((dept, idx) => (
src/components/tabs/DashboardTab.tsx:683:                          {REGION_ORDER.map((r: string, index: number) => (
src/components/tabs/DashboardTab.tsx:704:                          .map((p, index) => {
src/components/tabs/ResourcesTab.tsx:54:                      {DOC_CHECKLIST_ITEMS.map((item, idx) => (
src/components/tabs/ResourcesTab.tsx:121:                        ].map((doc, idx) => (
src/components/modals/BulkIssueModal.tsx:103:              ] as const).map(({ value, label }) => (
src/components/modals/UserManagementModal.tsx:63:                      ].map(p => (
src/components/modals/UserManagementModal.tsx:160:                    {projects.map((project, index) => {
src/components/modals/CreateApplicationModal.tsx:103:                          {visibleProjects.map((p, pIdx) => (
src/components/modals/ProjectModal.tsx:149:                  {(formData.originalDocumentChecklist || []).map((item, idx) => (
src/components/modals/ApplicationDetailModal.tsx:350:                      {['01', '02', '03', '04', '05', '06', '07'].map((label, idx) => {
src/components/modals/ApplicationDetailModal.tsx:542:                                     {['HĐMB/HĐCN Gốc', 'Văn bản chuyển nhượng', 'Lệ phí trước bạ', 'Sổ hộ khẩu/CCCD', 'Giấy xác nhận tình trạng hôn nhân'].map((item, idx) => {
src/components/modals/ApplicationDetailModal.tsx:899:                                             const h = (app.history || []).map(entry => ({
src/components/modals/ApplicationDetailModal.tsx:907:                                             const a = (app.auditTrail || []).map(entry => ({
src/components/modals/ApplicationDetailModal.tsx:930:                                             return merged.map((log, index) => (
src/components/modals/ApplicationDetailModal.tsx:985:                                       {(editApp || selectedApp).scannedFiles?.map((file, index) => (
src/components/modals/ImportPreviewModal.tsx:98:                {errors.map((e, i) => (
src/components/modals/ImportPreviewModal.tsx:111:                {duplicateFileWarnings.map((w, i) => (
src/components/modals/ImportPreviewModal.tsx:124:                {crossProjectWarnings.map((w, i) => (
src/components/modals/ImportPreviewModal.tsx:137:                {otherWarnings.map((w, i) => (
src/components/SettingsView.tsx:168:                  .map(([step, days]) => (
src/components/SettingsView.tsx:212:                  .map(([step, days]) => (
src/components/SettingsView.tsx:284:              {checklistTemplates.map((item, idx) => (
src/components/SettingsView.tsx:612:                {workflowSequences[workflowTab].map((key, index) => {
src/components/AppSubComponents.tsx:168:    {[1, 2, 3, 4, 5, 6].map((i) => (
src/components/ResourcesCenter.tsx:29:         {categories.map(cat => (
src/components/ResourcesCenter.tsx:66:           {[1, 2, 3, 4, 5].map(i => (
src/components/HandoverRecord.tsx:52:            {apps.map((app, idx) => (
src/components/HandoverRecord.tsx:64:                        {app.scannedFiles.map((f, fIdx) => (
src/components/HandoverRecord.tsx:75:                      {app.scannedFiles.map((f, fIdx) => (
src/components/HandoverRecord.tsx:91:            {apps.length === 0 && Array.from({length: 5}).map((_, i) => (
src/components/UserManagementView.tsx:60:            {users.map((user, idx) => (
src/components/ErrorReportView.tsx:34:    return applications.map(app => {
src/components/ErrorReportView.tsx:63:      .map(app => {
src/components/ErrorReportView.tsx:104:    return Object.entries(errorSummary).map(([key, value]) => ({
src/components/ErrorReportView.tsx:118:    return Object.entries(stepStats).map(([key, value]) => ({
src/components/ErrorReportView.tsx:266:                    {severityChartData.map((entry, index) => (
src/components/ErrorReportView.tsx:328:                    {typeChartData.map((entry, index) => (
src/components/ErrorReportView.tsx:366:                Object.keys(errorSummary).map((type, idx) => {
src/components/ErrorReportView.tsx:434:                  {stepChartData.map((entry, index) => (
src/components/ErrorReportView.tsx:485:                mappedAppsForIssues.map((app, index) => (
src/components/NotificationPanel.tsx:108:              {taskReminders.map((rem, index) => (
src/components/NotificationPanel.tsx:147:            displayedNotifications.map((n, idx) => (
src/components/FieldModeView.tsx:129:    return allowedProjects.map(p => p.name);
src/components/FieldModeView.tsx:436:          {allowedProjects.map((p, index) => (
src/components/FieldModeView.tsx:516:            filteredApps.map((app, index) => {
src/components/FieldModeView.tsx:818:                               ].map((st) => {
src/components/FieldModeView.tsx:1018:                                               {backwardSteps.map((st, idx) => (
src/components/FieldModeView.tsx:1074:                                  {appWorkflowSteps.map((stepKey, idx) => {
src/components/FieldModeView.tsx:1165:                                {editAppInstance.scannedFiles.map((file, i) => (
src/components/DashboardCharts.tsx:127:                  {chartData.map((entry, index) => (
src/components/DashboardCharts.tsx:137:                  {chartData.map((entry, index) => (
src/components/DashboardCharts.tsx:195:                  {pieData.map((entry, index) => (
src/components/DashboardCharts.tsx:229:            {pieData.map((entry, index) => (
src/components/LoginScreen.tsx:62:          assignedProjectIds: PROJECTS.map(p => p.id),
src/components/ReportsView.tsx:128:  const [selectedLoanProjectIds, setSelectedLoanProjectIds] = useState<string[]>(projects.map(p => p.id));
src/components/ReportsView.tsx:184:        ).map(p => {
src/components/ReportsView.tsx:232:        ...filteredApps.map(app => {
src/components/ReportsView.tsx:272:        ...filteredApps.map((app, i) => [
src/components/ReportsView.tsx:295:    const colWidths = (rows[3] || []).map((_: any, i: number) => ({
src/components/ReportsView.tsx:298:        ...rows.slice(3).map((r: any) => 
src/components/ReportsView.tsx:361:      return projects.map(p => {
src/components/ReportsView.tsx:374:      return REGION_ORDER.map(reg => {
src/components/ReportsView.tsx:389:      return projects.filter(p => selectedLoanProjectIds.includes(p.id)).map(p => {
src/components/ReportsView.tsx:404:      return depts.map(dept => {
src/components/ReportsView.tsx:411:           const overdue = applications.filter(app => app.id === a.id).map(app => {
src/components/ReportsView.tsx:451:       return Object.entries(userStats).map(([id, s]) => ({
src/components/ReportsView.tsx:466:    return steps.map(step => {
src/components/ReportsView.tsx:562:          {(Object.keys(reportConfig) as Array<keyof typeof reportConfig>).map((type, idx) => (
src/components/ReportsView.tsx:626:          {(Object.keys(reportConfig) as Array<keyof typeof reportConfig>).map((type, idx) => (
src/components/ReportsView.tsx:683:          {projects.map((p, pIdx) => (
src/components/ReportsView.tsx:746:                  {reportConfig[reportType].roles.map((r, idx) => (
src/components/ReportsView.tsx:753:               {reportConfig[reportType].kpis.map((kpi, i) => (
src/components/ReportsView.tsx:802:                         onClick={() => setSelectedLoanProjectIds(projects.map(p => p.id))}
src/components/ReportsView.tsx:813:                    {projects.map((p, pIdx) => {
src/components/ReportsView.tsx:893:                                 {loanPieData.map((entry: any, index: number) => (
src/components/ReportsView.tsx:949:                                 {loanPieData.map((entry: any, index: number) => (
src/components/ReportsView.tsx:991:                      {loanApps.map((app, index) => {
src/components/ReportsView.tsx:1116:                         {stats.length > 0 ? stats.slice().sort((a:any, b:any) => b.total - a.total).map((user: any, i: number) => (
src/components/ReportsView.tsx:1147:                    {stats.slice(0, 4).map((user: any, index: number) => (
src/components/ReportsView.tsx:1188:                            {slaStats.map((entry, index) => (
src/components/ReportsView.tsx:1199:                         {slaStats.slice().sort((a,b) => b.avgDays - a.avgDays).map((item, i) => (
src/components/ReportsView.tsx:1311:                {stats.slice(0, 4).sort((a,b) => b.completed - a.completed).map((p, i) => (
src/components/ProjectManagementView.tsx:100:          .map(([region, regionProjects], idx) => (
src/components/ProjectManagementView.tsx:145:                    {regionProjects.map((project, pIdx) => (
src/components/Sidebar.tsx:352:            .map(([region, regionProjects], regIndex) => (
src/components/Sidebar.tsx:398:                      {regionProjects.map((p, pIndex) => (
src/components/ThemeToggle.tsx:70:           {[...Array(6)].map((_, i) => (
src/utils/appUtils.ts:222:        .map(d => ({ ...d, value: app[d.key as keyof Application] }))
src/hooks/useExcelImport.ts:215:      data = sourceApps.map((app: Application) => [
src/hooks/useExcelImport.ts:244:      data = sourceApps.map((app: Application) => [
src/hooks/useExcelImport.ts:264:    worksheet['!cols'] = headers.map(() => ({ wch: 20 }));
src/hooks/useExcelImport.ts:851:        ...importPreviewData.toUpdate.map(t => t.app),
src/hooks/useExcelImport.ts:852:        ...importPreviewData.toCreate.map(t => t.app)
src/hooks/useBulkActions.ts:38:      const updatedApps = applications.map(app => {
src/hooks/useBulkActions.ts:69:      const updatedApps = appsToUpdate.map(app => 
src/App.tsx:247:      const healedApps = inconsistentApps.map(app => ({
src/App.tsx:263:          registerSelfUpdate(healedApps.map(a => a.id));
src/App.tsx:341:      .map(app => {
src/App.tsx:349:      .map(app => mapToSnakeCase(app));
src/App.tsx:511:      .map(fa => fa.id);
src/App.tsx:876:    return (dashboardApps || []).map(a => ({
src/App.tsx:1141:        if (usersData) setUsers(usersData.map(mapUserFromSnakeCase));
src/App.tsx:1168:      .map(p => p.name);
src/App.tsx:1282:              handleSetApplications(prev => prev.map(a => 
src/App.tsx:1285:              handleSetDashboardApps(prev => prev.map(a => 
src/App.tsx:1465:        const assignedNames = projects.filter(p => currentUser.assignedProjectIds?.includes(p.id)).map(p => p.name);
src/App.tsx:1752:      const fetchedApps = (data || []).map(mapFromSnakeCase);
src/App.tsx:1829:          const assignedNames = projects.filter(p => currentUser.assignedProjectIds.includes(p.id)).map(p => p.name);
src/App.tsx:1838:      const fetched = (data || []).map(mapFromSnakeCase);
src/App.tsx:1930:        .map(file => file.name);
src/App.tsx:1964:        setNotifications(data.map(mapNotificationFromSnakeCase));
src/App.tsx:2002:        targetUsers.map(u => createNotification({
src/App.tsx:2021:      setNotifications(prev => prev.map(n => n.id === id ? { ...n, isRead: true } : n));
src/App.tsx:2038:      setNotifications(prev => prev.map(n => ({ ...n, isRead: true })));
src/App.tsx:2356:          const allIds = displayedApps.map(a => a.id);
src/App.tsx:2369:            const dataRows = rowsToCopy.map(r => [
src/App.tsx:2463:      handleSetApplications(prev => prev.map(a => a.id === id ? finalApp : a));
src/App.tsx:2464:      handleSetDashboardApps(prev => prev.map(a => a.id === id ? finalApp : a));
src/App.tsx:2493:        const updatedApps = apps.map(app => {
src/App.tsx:2520:        const syncedApps = await Promise.all(updatedApps.map(app => localSyncRecord(app)));
src/App.tsx:2522:        handleSetApplications(prev => prev.map(a => {
src/App.tsx:2527:        handleSetDashboardApps(prev => prev.map(a => {
src/App.tsx:2659:          .map(d => ({ ...d, value: app[d.key as keyof Application] }))
src/App.tsx:2946:        const finalUpdatedApps = await bulkSyncRecordsToSupabase(updatePayloads.map(p => mapFromSnakeCase(p)), updatedAppsLocal);
src/App.tsx:3126:      data = sourceApps.map(app => [
src/App.tsx:3156:      data = sourceApps.map(app => {
src/App.tsx:3183:      data = sourceApps.map(app => [
src/App.tsx:3211:      data = sourceApps.map(app => [
src/App.tsx:3231:      data = sourceApps.map(app => [
src/App.tsx:3356:      handleSetApplications(prev => prev.map(app => app.id === oldId ? finalApp : app));
src/App.tsx:3357:      handleSetDashboardApps(prev => prev.map(app => app.id === oldId ? finalApp : app));
src/App.tsx:3556:      handleSetApplications(prev => prev.map(a => a.id === app.id ? finalApp : a));
src/App.tsx:3557:      handleSetDashboardApps(prev => prev.map(a => a.id === app.id ? finalApp : a));
src/App.tsx:3668:      const updatedApps = applications.map(app => {
src/App.tsx:3868:      await Promise.all(appsToSync.map(app => notifyNextDepartment(app, app.currentStep)));
src/App.tsx:3872:        await Promise.all(selectedAppIds.map(id => deleteAllNotificationsForRecord(id)));
src/App.tsx:4015:      const updatedApps = applications.map(a => a.id === app.id ? finalApp : a);
src/App.tsx:4017:      handleSetDashboardApps(prev => prev.map(a => a.id === app.id ? finalApp : a));
src/App.tsx:4061:      const updatedApps = applications.map(a => a.id === app.id ? finalApp : a);
src/App.tsx:4063:      handleSetDashboardApps(prev => prev.map(a => a.id === app.id ? finalApp : a));
src/App.tsx:4113:        .map(app => ({
src/App.tsx:4129:      handleSetDashboardApps(prev => prev.map(a => {
src/App.tsx:4211:      handleSetApplications(prev => prev.map(a => a.id === app.id ? finalApp : a));
src/App.tsx:4260:      handleSetApplications(prev => prev.map(a => a.id === app.id ? finalApp : a));
src/App.tsx:4307:      handleSetApplications(prev => prev.map(a => a.id === appId ? finalApp : a));
src/App.tsx:4308:      handleSetDashboardApps(prev => prev.map(a => a.id === appId ? finalApp : a));
src/App.tsx:4361:      const promises = targetUsers.map(u => 
src/App.tsx:4375:      handleSetApplications(prev => prev.map(a => a.id === oldId ? finalApp : a));
src/App.tsx:4376:      handleSetDashboardApps(prev => prev.map(a => a.id === oldId ? finalApp : a));
src/App.tsx:4585:      handleSetApplications(prev => prev.map(app => {
src/App.tsx:4837:      setUsers(prev => prev.map(u => u.id === editUser.id ? editUser : u));
src/App.tsx:4872:      setUsers(prev => prev.map(usr => usr.id === u.id ? updatedUser : usr));
src/App.tsx:4937:        .map(a => ({ ...a, overdue: getOverdueInfo(a, stepConfig, slaConfig) }))
src/App.tsx:5006:    const deptStats = depts.map(dept => {
src/App.tsx:5037:    const adminSlaStats = adminSlaStages.map(stage => {
src/App.tsx:5224:            setSelectedAppIds(displayedApps.map(a => a.id));
src/App.tsx:5410:    return chartData.map(d => {
src/App.tsx:5425:    return chartData.map(d => ({
src/App.tsx:5497:          handleSetApplications(prev => prev.map(a => a.id === updated.id ? updated : a));
src/App.tsx:5828:                {(currentUser?.name || 'User').split(' ').map(n => n[0]).join('').slice(0, 2)}
src/App.tsx:6229:                updatedProjects = projects.map(proj => proj.id === editingProject.id ? updated as Project : proj);
src/App.tsx:6288:        unitCodes={applications.filter(a => selectedAppIds.includes(a.id)).map(a => a.unitCode)}
src/App.tsx:6307:        unitCodes={applications.filter(a => selectedAppIds.includes(a.id)).map(a => a.unitCode)}
src/App.tsx:6420:                        {visibleProjects.map((p, index) => (
